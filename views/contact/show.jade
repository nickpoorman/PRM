extends ../layout

block content
  .container
    // contact show
    ul
      li= contact.firstName + ' ' + contact.lastName
      li= contact.company
      each phone in contact.phones
        li= phone.label + ' ' + phone.value
    // end contact show